{
  "name": "youtube-api-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "cd src && tsc --watch --preserveWatchOutput",
    "test": "echo \"Error: no test specified\" && exit 1",
    "save": "git add . && git commit && git push origin main"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^10.0.0",
    "google-auth-library": "^7.10.1",
    "googleapis": "^89.0.0"
  },
  "devDependencies": {
    "@types/node": "^17.0.8"
  }
}